# MEMORY.md · 洪荒 - 世界（长期记忆）

## 项目定位（2026-08-27 界主澄清，权威）
- **AI 原生开发项目**：一切以 AI 开发场景为主。完全由 AI 编码，人（界主）只负责提需求与验收。
- 由此推导的约定（与常规人写项目相反）：
  - 全中文命名（维度→域→府→殿→阁→园 六层 + lib 拼音名）**不是工程负担**，而是「人验友好的可审计性设计」，保持。
  - 传承殿文档体系（宪法/契约/决策/复用经验）是给 AI 的**规格书与上下文**，属核心资产，应持续强化而非「瘦身」。
  - 改进重心从「降低人力维护成本」转向「提升验收可量化度、规格无歧义、AI 产出可验证」。

## 技术栈与铁律（来自用户规则，权威源）
- 9 根维度 + 6 层结构（维度→域→府→殿→阁→园，禁跳层/深链/../）。
- 督察 CLI 21 规则 + 37 违规模式 + 错题本/正确本。
- 生产禁 panic/unwrap/expect/todo；密钥用环境变量；原子写(.tmp→rename)；单条失败不中断整体；声明必调用；能力零互指。
- 术语铁律：项目「能力」(能力-*.rs 运行时模块) ≠ 开发「工具」(督察CLI/cargo/测试)。表述严格区分。

## 用户偏好
- 界主用中文交互；AI 自行管理项目记忆（`.codebuddy/memory/`）。
- 当前 OS：win32（PowerShell Core）；Rust workspace，rust-version 1.75。
