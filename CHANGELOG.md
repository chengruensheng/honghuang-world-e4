# 变更日志

> **原语**：本项目沿用的创世叙事词汇，仅用于命名与溯源。
> **工程理解词语**：将原语翻译为可精确执行、可验证的工程术语；本文正文一律使用工程理解词语，原语仅在《术语对照表》中保留。
> 术语对照见 `传承殿/00-宪法/术语对照表.md`。

本文件记录「洪荒 · 世界」项目自诞生以来的关键变更，格式遵循
[Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 约定，版本号遵循语义化版本（SemVer）。

> 说明：项目早期迭代曾出现版本号漂移，自 **v0.2.0** 起统一为语义化版本，当前权威版本号为 **v0.2.0**。

## [v0.2.0] - 2026-09-02（已发布）

阶段 3「自举」：项目完成自我开发能力、真实生产流水线与记忆架构契约的落地，形成「可理解、可构建、可运行、可自举」的分发级形态。

### 新增

- 记忆架构契约九节落地：上下文槽位 / 双工流水线 / 任务债务 / 人类最终验收授权 / 回填 / 降级快照持久化。
- 36 上下文槽位写入 / 读取闭环 API（槽位中枢），关键词表可配置化，永驻加载 36 行摘要。
- 记忆四分类工具（写 / 读 / 查 / 永驻摘要），槽位级仓库分区检索，写入按槽位。
- 记忆工具系列：确认槽位记忆（授权）、登记世界事实、读取三档投影、播种槽位 36、精炼、会话记录、事件流、完成度刻度、槽位统计、世界快照、任务收尾。
- 门禁扩展：槽位稀缺 36 上限、文档改动校验门禁（完整验证流水线 13 → 15 项）。
- 阶段 B 门禁扩展：完整验证流水线扩展至 **17 项**，新增治理审计、标识符中文门禁。
- 工具府落地：mock 工具升级为真实工具 + 安全白名单。
- 自举能力：任务单、自举执行、端到端拆分、生产 CLI 自举命令。
- 真实生产流水线：生产 CLI，真实 LLM 端到端首次跑通，默认真实 API，4 分类接力协作。
- 13 过程态转移判定器（推进 + 不可跳层校验 + 职责态映射）。

### 可用性打磨（2026-09-02）

- 生产 CLI 首次启动加载工作区根 `.env`：根治「用户已配置 key 但 CLI 仍报未设置」缺陷，真实后端从此可跑通（对照验证：无 .env fail-loud 退出码 4 / 有 .env 发起真实请求）。
- 流水线实时进度输出（stderr）：真实 LLM 单轮最长 120s，此前全程静默易误判卡死；现逐阶段输出「启动 / 各角色思考 / 完成 / 终裁」，`LLM_PROGRESS=off` 可关闭。
- HTTP 连接补 `timeout_connect`：DNS/TCP 握手阶段同样受超时约束（此前仅读/写超时）。
- 根治一键全验 PowerShell 5.1 NativeCommandError 误判：6 个门禁脚本 `$ErrorActionPreference` Stop→Continue + cargo 调用 `2>&1`，修后一键全验 **17/17 全绿**。
- CI 一键全验按平台分叉：Ubuntu 用 `bash ./一键全验.sh`（pwsh 已装），Windows 用 `./一键全验.ps1`（runner 自带 PowerShell 5.1），根治 Windows runner 双平台失败。
- README 新增「快速开始（5 分钟）」：构建 → 零配置 mock demo → 健康检查三步走；新增环境变量表与 `.env.example` 配置模板。
- 门户工作台（Web 门面）实测：6 个 API 端点全部返回真实数据（98 事件 / 18 任务全交付 / 111 记忆 / 仙官名册 / 切面四大类）。
- 新增 `洪荒 探针` 子命令（真实 LLM 连通性体检）：发 1 token 最小请求验证 key/端点/模型/网络全链路，分档诊断（key 缺失三步指引 / 401 / 402 / 超时 / 端点格式），退出码对齐约定（0=连通，4=模型故障）；单测用本地假 LLM 服务器覆盖连通与请求体格式。实测：本机真实连通 MiniMax-M3（首次真实 LLM 端到端跑通，181 in / 16 out token），无 key 时退出码 4 且输出不泄露密钥。

### 变更

- 记忆模型由「6 分区 × 6 子槽」迁移为「6 总纲 × 36 本质」（阶段独立决策状态机）。

## [v0.1.0] - 2026-08-27

可用阶段达成：完成六层结构统一、真实云端 LLM 接入、端到端自开发能力验证，项目达到「让 LLM 自己写一个新 crate + 自动 commit」的雏形。

### 新增

- 六层结构命名唯一化，消除殿 / 阁 / 园重复命名。
- 命名唯一性门禁（祖孙不同名 + 同层全局唯一 + 无英文白名单），完整验证流水线扩至 12 项。
- 全部府级 crate 切分为 ≥2 殿六层风格。
- 防退化 ≥2 殿门禁，决策契约接入。
- 真实云端 LLM 接入：Bearer 认证 + Base URL + 4 分类各自模型 + 优雅降级。
- CI 完整验证流水线 13 项 + GitHub Actions matrix（ubuntu + windows 双 runner）。
- 端到端自开发能力验证（LLM 生成新 crate + cargo check + 测试通过）。
- 自检命令（聚合门禁，AI 助手自给自足）。

### 修复

- 修复写 / 查询 / 计数祖孙语义重复（阁名与园名不再同名）。

## [v0.0.1] - 2026-08-27

项目奠基：从 workspace 空骨架起步，经历阶段 1-18，形成治理内核与 4 分类 LLM 流水线雏形。

### 新增

- Rust workspace 空骨架 + 完整验证流水线 10 项。
- 事件流 + 插件上下文：哈希链 + Waterfall/Serial + 类型擦除查找 + manifest 解析。
- 记忆承载：四维正交 + 36 上下文槽位 + 加载档位协议。
- 规则府：规则注册表 + 决策契约校验。
- 4 分类状态机 + 4 角色卡。
- 3 CLI 命令 + e2e 流水线。
- LLM 调用层：HTTP POST + 4 分类 LLM 池 + 5 类工具 + mock e2e。
- 追问引擎：4 问题枚举 + 多 LLM 投票 + 共识升级。
- 监控：4 类指标 + 4 级告警 + 4 级应急。
- 升级府：3 类升级策略 + 复用经验入 36 上下文槽位。
- SQLite 后端 + 100% 恢复。
- 规则注册表接入写入路径。
- 架构校验脚本（PowerShell 11 项）。
- v4 阶段 15-18：性能基准、架构校验 Rust 化、mock HTTP 服务器、多 agent 协同。

---

*洪荒 · 世界 · CHANGELOG · decided_by: 项目负责人*
