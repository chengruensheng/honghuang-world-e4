# 变更日志

本文件记录「洪荒 · 世界」项目自诞生以来的关键变更，格式遵循
[Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 约定，
版本号遵循语义化版本（SemVer）。

> 说明：项目早期迭代曾出现版本号漂移（历史标签 `v0.10.0`、`v1.0.0`、`V0.0.1.0` 并存），
> 自 **v0.2.0** 起统一为语义化版本，当前权威版本号为 **v0.2.0**。

## [v0.2.0] - 2026-08-30（未发布）

阶段 3「自举」：项目完成自我开发能力、真实生产流水线与记忆架构契约的落地，
形成「可理解、可构建、可运行、可自举」的分发级形态。

### 新增

- 记忆架构契约九节落地：格位 / 双工 / 债务 / 玉玺 / 回填 / 降级快照持久化。
- 36 格位写入 / 读取闭环 API（格位中枢），匹配范畴关键词可配置化，永驻加载 36 行摘要。
- 记忆四分类工具（写 / 读 / 查 / 永驻摘要），格位级仓库分区检索，写入按格位。
- 记忆工具系列：确认格位记忆（盖玉玺）、登记世界事实、读取三档投影、播种格位 36、
  地道精炼、会话记录、事件流、完成度刻度、格位统计、世界快照、任务收尾。
- 记忆命令（`记忆 <三档|查|播种|精炼|会话|事件|完成度|统计|世界快照|收尾>`），
  支持 AI 无人自主开发（无玉玺可执行）。
- 门禁扩展：格位稀缺 36 上限、文档收割门防污染（一键全验 13 → 15 项）。
- 自检第 14 / 15 项：任务收尾三件套、一键全验双版本引用完整性。
- 阶段 B 门禁扩展：一键全验扩展至 **17 项**，新增治理审计、标识符中文门禁。
- 工具府落地（阶段 0）：5 殿 10 阁，mock 工具升级为真实工具 + 安全白名单。
- 自举能力（阶段 1-4）：任务单、自举执行、端到端拆分、生产 CLI 自举命令。
- 自举配套成果：角色卡规则格位化、标识符中文化、版本成员漂移修复、校验和样本。
- 真实生产流水线：世界 bin 升级为生产 CLI，真实 LLM 端到端首次跑通，
  默认真实 API（严禁降级 mock），run 命令接入记忆闭环，4 分类接力协作。
- 13 过程态转移判定器（推进 + 不可跳层校验 + 职责态映射）。

### 变更

- 记忆范畴由「6 范畴 × 6 阶段」迁移为「6 总纲 × 36 本质」（阶段独立决策状态机）。
- 测试临时 SQLite 库改写入系统临时目录，杜绝污染项目目录。

## [v0.1.0] - 2026-08-27

可用阶段达成：完成六层结构统一、真实云端 LLM 接入、端到端自开发能力验证，
项目达到「让 LLM 自己写一个新 crate + 自动 commit」的雏形。

### 新增

- 六层结构命名唯一化：按六层规范消除殿 / 阁 / 园重复命名。
- 命名唯一性门禁（祖孙不同名 + 同层全局唯一 + 无英文白名单），一键全验扩至 12 项。
- 21 府级 crate 全部切分为 ≥2 殿六层风格（调遣 / 评估 / 状态共享 / 观测探针 /
  单元测试 / 配置管理 / 日志记录 / 世界 / 命令操作 / 实时）。
- 防退化 ≥2 殿门禁，决策契约接入。
- 真实云端 LLM 接入（环境配置-殿）：Bearer 认证 + Base URL + 4 分类各自模型 + 优雅降级。
- CI 一键全验 13 项 + GitHub Actions matrix（ubuntu + windows 双 runner）。
- moxing_fu 调用方集成（真实-后端-选择-殿）：后端模式枚举、环境变量后端、端点配置。
- 端到端自开发能力验证（minimax LLM 生成 工具-hello-府 + cargo check + 测试通过）。
- 自检命令（聚合 13 项门禁，AI 助手自给自足）。
- 可用阶段达成：10 个工具 crate（加密 / 日志 / 配置 / 时钟 / 路径 / 随机 / 压缩 /
  哈希 / 正则 / 时间戳），280 测试 + 13/13 门禁。
- 真实 LLM 完整 4 分类流水线，SQLite 存储移出 `cfg(test)` 供 36 格位应用。

### 修复

- 修复写 / 查询 / 计数祖孙语义重复（阁名与园名不再同名）。

## [v0.0.1] - 2026-08-27

项目奠基：从 workspace 空骨架起步，经历阶段 1-18，形成治理内核与 4 分类 LLM 流水线雏形。

### 新增

- Rust workspace + 12 crate 空骨架 + 一键全验 10 项。
- 事件流 + 插件上下文：hash 链 + Waterfall/Serial + 类型擦除查找 + manifest 解析。
- 记忆承载（阶段 3）：四维正交 + 36 格位 + 加载档位协议。
- 规则-府（阶段 4）：RULE_REGISTRY 14 条规则 + 决策契约 lint。
- 4 分类状态机 + 4 角色卡（阶段 5）。
- 3 CLI 命令 + e2e 流水线（阶段 6）。
- moxing_fu：LLM HTTP POST + 4 分类 LLM 池 + 5 类工具 + mock e2e（阶段 7）。
- 追问引擎（阶段 8）：4 问题枚举 + 多 LLM 投票 + 共识升级。
- 监控（阶段 9）：4 类指标 + 4 级告警 + 4 级应急。
- 升级-府（阶段 10）：3 类升级策略 + 复用经验入 36 格位。
- SQLite 后端 + 100% 恢复。
- RULE_REGISTRY 接入写入路径（guize_fu）。
- 架构校验脚本（PowerShell 11 项）。
- v4 阶段 15-18：性能基准（chengben_fu）、架构校验 Rust 化（jianyan_gongju 15/15）、
  mock HTTP 服务器（shishi_fu）、多 agent 协同（diaoqian_fu）。
