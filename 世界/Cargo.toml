[package]
name = "shijie"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "世界 - 二进位入口（bin），阶段 1 仅承载工作空间收敛"

[lib]
name = "shijie"
path = "入口.rs"

[[bin]]
name = "shijie"
path = "二进制入口.rs"

[dependencies]
# 真实生产流程：世界 bin 编排命令操作-府 CLI（run 走真实 LLM 后端）
mingling_caozuo_fu = { path = "../乾坤/界面呈现-域/命令操作-府" }
