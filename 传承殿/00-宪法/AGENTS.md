# AGENTS.md · v3

> **元层**（治理的治理）—— 一切文档的最高优先。本文件约束传承殿所有 AI 助手与人类协作者的行为。

## 一、来源声明

- **5 工程基线 + 4 禁止条款**：来自司衡工程哲学（参考 `08-参考/司衡仓库/`）
- **万物皆插件 + Waterfall + frozen outcome**：来自 DeepSeek Harness 架构（参考 `08-参考/DSH-架构/`）
- **9 根维度 + 6 层结构 + 4 分类抽象**：传承殿世界观（参考 `01-哲学/01-世界观.md`）

## 二、铁律（17 条 = 5 基线 + 4 禁止 + 5 决策 + 3 命名）

### A. 5 工程基线（来自司衡）

1. **确定程序是治理操作唯一执行者**——LLM 只生成符号材料，不拥有写入知识包 / 事件流 / 决策契约的权限。
2. **信息洪流是旧仓失败根因**——治理系统必须假设人类注意力稀缺，将介入点限制在异常。
3. **人类只看异常，不看原始日志**——所有 LLM 输出需有摘要 + 异常门自动顶人类。
4. **可验证性约束**——操作来源可追溯、结果可机械校验、历史不可篡改（frozen outcome）。
5. **治理延伸是减少 LLM 参与**——关键决策（接单门/验收打回/终裁）必须确定化。

### B. 4 禁止条款（来自司衡）

6. **LLM 不可直改知识包**——所有写入需经 decided_by 字段校验。
7. **不可复现多 Agent 交互不可作治理决策依据**——关键决策需多 LLM 投票 + 阈值。
8. **治理操作必入事件流，留痕不可篡改**——frozen outcome + hash 链。
9. **人类只通过视图介入**——界主不直接看原始对话，只看异常告警。

### C. 5 决策契约（v3 新增）

10. **每决策带 decided_by**——缺失则事件拒收。
11. **每决策带 falsifiable**——可证伪命题 + 证伪方法 + 时间窗口。
12. **每决策可追溯到哲学锚**——implements 字段必须引用道/法/术/鉴/应/元之一。
13. **先入稿再落码**——新设计先入对应设计稿章节，再实现。
14. **文档收割门**——AI 改任何 .md 必须 diff 对比；`[object Object]` / 内容断崖缩水 / 章节级损坏 → 立即回滚。

### D. 3 命名规则（v3 新增）

15. **不沿用借来的词**——拒绝"乐高"、"工具 manifest"等未推导的命名；每个命名必须能回答"为什么叫这个"。
16. **哲学推导优先**——每个概念必须先写哲学层（为什么）→ 概念层（是什么）→ 工程层（怎么实现）。
17. **中文标识符**——目录用连字符，Rust 模块用下划线，`#[path]` 桥接。
18. **严禁 src/ 平铺**——每个 Rust crate 内部只能有 src/lib.rs（入口）或 src/模块名/mod.rs（嵌套模块），禁止 src/foo.rs 平铺；项目根目录严禁 src/ 目录。

## 三、不变量

- 完成度刻度：**带「占位」的入口 = 未完成**；质量唯一标尺 = `cargo test --workspace` 全绿 + `cargo clippy --workspace -- -D warnings` 零警告 + `bash 一键全验.sh` 10/10 全绿。
- 一旦决策拍板（带 decided_by），**不可被推翻除非新决策覆盖**——所有推翻需新 falsifiable 论证。

## 四、决策记录

所有对本文件的修改必须以决策文档形式留痕（见 `03-决策/00-决策契约.md`）。不接受"口头修改"。

---

*传承殿 · 2026-08-26 · decided_by: 界主*
*implements: 元·元（治理的治理）*
*falsifiable: 上线 3 个月，17 条铁律的违反率 < 5%*