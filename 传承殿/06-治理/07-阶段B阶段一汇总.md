# 阶段 B 阶段一汇总（真实端到端回归 1-20）

> decided_by：界主（AI 自主落地）
> falsifiable：4 共同样本真实回归全部产出评分 100/100、一键全验 17/17、债务 0
> implements：术（真实生产流程端到端验证与四要素质量门禁落地）

## 一、验收结论

阶段一（真实端到端回归 1-20）核心达成：4 共同样本真实 API 回归全部产出评分 **100/100**，阶段 B 的 B1 falsifiable「≥75% 评分 100」超额达成（100%）。

| 样本 | 首跑 | 修复后 | 产出评分 |
|---|---|---|---|
| 整数加法 | ✅ | ✅ | 100/100 |
| 字符串反转 | ✅ | ✅ | 100/100 |
| 校验和 | ❌ 打回×2 | ✅ | 100/100 |
| 列表去重 | ❌ 零交付打回 | ✅ | 100/100 |

## 二、真实回归暴露的问题（本轮核心价值）

首跑通过率仅 50%，真实 API 非确定性暴露三处缺口：

1. 校验和：大罗级产出缺 decided_by/falsifiable（质量门禁正确拦截，打回×2）。
2. 列表去重：大罗级零交付（仅声明未执行，性质判定零交付打回）。
3. 打回上限默认 1 无重投机会，真实 LLM 一次失误即终局。

## 三、架构级修复（非补丁，融入流水线铁律）

1. 大罗角色卡第 ④ 铁律扩展为四要素：decided_by + falsifiable + implements + 复现，缺一即打回（分类_核心.rs）。
2. 质量门禁校验两必填 → 四要素（终裁判定.rs）：消除「评分 80 仍交付」的漏洞，质量门禁必填集与产出评分 100 要素集对齐。
3. 打回上限默认 1 → 3（端点配置.rs 三处）：对齐契约「可循环打回 ≤3 轮」。
4. 材料模板防退化测试三要素 → 四要素（材料模板_核心.rs）。
5. 播种真实资产「默认 1」→「默认 3」并重新播种记忆库。

## 四、温度扫描结论（任务 15）

| 终裁温度 | 结果 | 判定 |
|---|---|---|
| 0.1（低温） | 校验和打回达上限（3 轮重投全败） | 低温确定性高但质量差 |
| 0.3（默认） | 4 样本全 100 | 稳定档，无需调参 |
| 0.7（高温） | 打回 / 超时 | 高温发散 |

结论：默认终裁温度 0.3 已是最优稳定档；打回上限 3 在低温下正确触发「打回达上限」。

## 五、状态实测

- 总条数 97（4 样本真实交付回填新增 8 条）
- 债务 0（交付→归档闭环归零）
- 治理事件流 38 条（6 类：终裁通过交付/终裁打回/打回重投/打回达上限/玉玺盖印/废止）
- 33 crate 测试绿 + clippy 零 + fmt 零 + 一键全验 17/17

## 六、逐项清单同步

- 第 10 项「产出三要素完整」→「产出四要素完整」（+复现）
- 第 14 项「默认上限为 1」→「默认上限为 3」（对齐契约）

## 七、遗留

- 任务 16（多次采样多数裁决）：真实采样次数=3 中断未完成；采样次数=1 已由 4 样本稳定证明，多数裁决纯函数已有单测覆盖（终裁判定模块），标记低优先。
