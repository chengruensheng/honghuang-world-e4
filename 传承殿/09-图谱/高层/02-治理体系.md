# 02-治理体系

> 高层视图——司衡基线 + DSH 标准 + 三维正交记忆模型。

## 一、司衡 5 基线 + 4 禁止 + 5 法 + 元三治

```mermaid
mindmap
  root((司衡工程哲学))
    5 工程基线
      确定程序治理
      信息洪流防护
      人类只看异常
      可验证性约束
      减少 LLM 参与
    4 禁止条款
      LLM 不直改知识包
      不可复现不治理
      治理必入事件流
      人类只看视图
    5 法
      顺因
      有度
      知止
      损补
      顺势
    元三治
      治病
      治益
      治强
```

## 二、DSH 6 子系统 + 3 类事件 + 2 模式

```mermaid
flowchart LR
    subgraph 6子系统["DSH 6 子系统"]
        session[session<br/>会话日志]:::core
        prompt[system-prompt<br/>提示组装]
        tools[tools<br/>工具注册表]:::core
        agent[agent<br/>Agent 接口]:::core
        loop[agent-loop<br/>默认 driver]:::core
        scope[scope<br/>作用域原语]
    end

    subgraph 3类事件["3 类事件"]
        sessionEv[Session 事件<br/>持久化]:::ev
        agentEv[Agent 事件<br/>live]
        capEv[Capability 事件<br/>policy + adapter]
    end

    subgraph 2模式["2 模式"]
        water[Waterfall<br/>必须 next\(\) 链]
        serial[Serial<br/>独立拦截点]
    end

    session --> sessionEv
    agent --> agentEv
    tools --> capEv
    session --> water
    agent --> water
    prompt --> water
    tools --> water

    classDef core fill:#e1f5e1,stroke:#0a0
    classDef ev fill:#fff5e1,stroke:#a50
```

## 三、三维正交记忆模型

```mermaid
flowchart TB
    subgraph 范畴["范畴维度（写什么）"]
        目标[目标]
        规则[规则]
        自我[自我]
        程序[程序]
        世界[世界]
        经历[经历]
    end

    subgraph 时间["时间维度（怎么用）"]
        经档[经档<br/>永久不变]:::perm
        权档[权档<br/>当前 session]:::active
        行档[行档<br/>session 内]:::temp
    end

    subgraph 来源["来源维度（谁写的）"]
        代码[代码]:::src
        LLM[LLM]:::src
        人类[人类]:::src
    end

    条目["记忆条目<br/>= 范畴 + 时间 + 来源<br/>三维正交"]
    范畴 --> 条目
    时间 --> 条目
    来源 --> 条目

    classDef perm fill:#e1e1ff,stroke:#00a
    classDef active fill:#fff5e1,stroke:#a50
    classDef temp fill:#ffe1e1,stroke:#a00
    classDef src fill:#f0f0f0,stroke:#666
```

## 四、治理体系整体架构

```mermaid
flowchart TB
    哲学[司衡哲学<br/>5 基线 + 4 禁止 + 5 法 + 元三治]:::phi
    标准[DSH 工程标准<br/>万物皆插件 + Waterfall + frozen outcome]:::dsh
    记忆[三维正交记忆<br/>36 格位 × 3 档 × 3 源]:::mem
    决策[决策契约<br/>decided_by + falsifiable + implements]:::dec
    命名[命名纪律<br/>哲学推导 + 不借词]:::name

    哲学 --> 决策
    标准 --> 决策
    决策 --> 记忆
    命名 --> 决策

    classDef phi fill:#e1e1ff,stroke:#00a,stroke-width:2px
    classDef dsh fill:#fff5e1,stroke:#a50,stroke-width:2px
    classDef mem fill:#e1f5e1,stroke:#0a0,stroke-width:2px
    classDef dec fill:#ffe1e1,stroke:#a00,stroke-width:2px
    classDef name fill:#f0f0f0,stroke:#666,stroke-width:2px
```

## falsifiable

- 上线 1 个月：图谱与治理文档 100% 一致
- 上线 3 个月：新人通过此图能背诵 5 基线 + 4 禁止

---

*传承殿 · 2026-08-26 · decided_by: 界主*
*implements: 应（治理体系全貌）*
