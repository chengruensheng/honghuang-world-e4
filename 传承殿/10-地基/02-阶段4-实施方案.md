# 阶段 4 实施方案：决策契约 + RULE_REGISTRY

> **入稿日期**：2026-08-26
> **执行人**：MiniMax-M3
> **决策锚**：260826-2220 治理-司衡 + 260826-2240 传承殿启动
> **状态**：进行中

## 一、目标与 falsifiable

### 1.1 阶段目标

建立可证伪 + 可演化的治理基础：
- RULE_REGISTRY 单一真相源（14 条规则 = 13 接单门 + 1 V-COUNT）
- 决策契约强制校验（decided_by + falsifiable + implements + upstream 必填）
- 决策文档 lint 脚本（缺字段立即阻断）

### 1.2 falsifiable 验证清单

- RULE_COUNT = RULE_REGISTRY.len()（派生值不硬编码）
- decided_by 缺失写入失败率 = 100%
- 14 条规则全部在 RULE_REGISTRY 中
- 决策文档 YAML frontmatter 7 字段缺失立即报错

## 二、设计依据

- 概念：`02-概念/规则注册表/06-规则注册表.md`
- 决策契约：`00-宪法/DECISION-CONTRACT.md`（7 字段：id/title/stage/decided_by/falsifiable/upstream/implements/decided_at）
- 治理决策：`03-决策/已定/260826-2220-治理-司衡.md`（5 基线 + 4 禁止）
- 命名哲学：AGENTS § 17 中文标识符

## 三、13 接单门清单（按 AGENTS § 17 拆解）

### A. 5 工程基线（司衡承接）

| 规则 ID | 严格度 | 治理域 | 内容 |
|:--|:--|:--|:--|
| BASE-001-程序定 | Fatal | 治理 | 确定程序是治理操作唯一执行者（LLM 只生成符号） |
| BASE-002-异常门 | Warning | 派发 | 异常门自动顶异常（人类只看异常） |
| BASE-003-可验证 | Fatal | 结构 | 可验证性约束（hash + 落事件流 + falsifiable） |
| BASE-004-减LLM | Info | 派发 | 治理延伸是减少 LLM 参与（关键决策确定化） |
| BASE-005-异常优先 | Warning | 派发 | 信息洪流是旧仓失败根因（异常优先） |

### B. 4 禁止条款（司衡承接）

| 规则 ID | 严格度 | 治理域 | 内容 |
|:--|:--|:--|:--|
| BAN-001-LLM直改 | Fatal | 治理 | LLM 不可直改知识包（decided_by 必填） |
| BAN-002-不可复现 | Fatal | 引用 | 不可复现多 Agent 交互不可作治理决策依据 |
| BAN-003-入事件流 | Fatal | 结构 | 治理操作必入事件流，留痕不可篡改 |
| BAN-004-视图介入 | Warning | 派发 | 人类只通过视图介入 |

### C. 4 决策契约（v3 新增）

| 规则 ID | 严格度 | 治理域 | 内容 |
|:--|:--|:--|:--|
| CONTRACT-001-decidedBy | Fatal | 前置 | 每决策带 decided_by（缺失则事件拒收） |
| CONTRACT-002-falsifiable | Fatal | 前置 | 每决策带 falsifiable（可证伪命题 + 时间窗口） |
| CONTRACT-003-implements | Warning | 引用 | 每决策可追溯到哲学锚（implements 字段） |
| CONTRACT-004-入稿落码 | Info | 前置 | 先入稿再落码（AGENTS § 8） |

### D. V-COUNT 派生规则

- RULE-V-COUNT（Warning | 结构）：RULE_REGISTRY.len() == 13（V-COUNT 不计入自身）

合计：13 接单门 + 1 V-COUNT = 14 规则。

## 四、实施任务（5 天）

### Day 1：新建 道韵/规则-域/规则-府 crate + RULE_REGISTRY 实现

- 新建 crate `guize_fu`（pinyin 名 = "规则-府"）
- `pub struct 规则条目 { 规则ID, 严格度, 治理域, 描述, decided_by, falsifiable: Vec<可证伪条件>, implements }`
- `pub enum 严格度 { Fatal, Warning, Info }`
- `pub enum 治理域 { 前置, 结构, 引用, 治理, 派发 }`
- `pub const RULE_REGISTRY: &[规则条目] = &[...14 条...] as const;`
- `pub const RULE_COUNT: usize = RULE_REGISTRY.len();`
- 单元测试：RULE_COUNT == 14、规则 ID 唯一、所有规则都有 decided_by

### Day 2：评估接单 + 决策契约字段校验

- `pub fn 评估接单(候选: &接单候选) -> 接单决策 { 接受 | 拒绝{规则ID} }`：遍历 RULE_REGISTRY，对每条规则调用对应 `违反()` 函数
- 决策契约字段校验：
  - `校验决策契约(doc: &str) -> Result<(), Vec<缺失字段>>`
  - 必填 7 字段：id / title / stage / decided_by / falsifiable / upstream / implements / decided_at
- 单元测试：每条规则违反时返回正确拒绝原因；缺字段全部报错
### Day 3：决策契约文档 lint 脚本

- 新增 `道果树/质量门禁-域/门禁-府/验证-决策契约.ps1`
- 扫描 `传承殿/03-决策/**/*.md`
- 用正则提取 YAML frontmatter
- 校验 7 必填字段（id/title/stage/decided_by/falsifiable/upstream/implements/decided_at）
- 缺字段即报错（含文件名 + 缺字段列表）
- 纳入 `一键全验.sh` 第 11 项门禁（更新 10 → 11）

### Day 4：写入接口拦截 + 集成

- 在 `jiyi_chengzai_fu::内存存储::写入校验` 增加 decided_by 校验（已实现）
- 在 `kuawai_shijian_zongxian_fu::事件流::写入` 增加 decided_by 校验
- 决策契约文档写入接口（写入 03-决策/已定/*.md）必经字段校验

### Day 5：端到端 + commit

- 集成测试：
  - 14 条规则遍历评估 → 100% 命中
  - 决策文档缺字段 → 100% 报错
  - 写入接口缺 decided_by → 100% 拒绝
- 一键全验 11/11 全绿
- commit 阶段 4

## 五、命名与代码风格契约

- 目录：道韵/规则-域/规则-府/（中文 + 连字符）
- Rust lib 名：`guize_fu`（pinyin_xxx_fu 风格）
- 文件：`入口.rs`（无 src/ 平铺）
- 注释：中文，含决策锚
- 规则 ID：`BASE-001-xxx` / `BAN-001-xxx` / `CONTRACT-001-xxx` / `RULE-V-COUNT`（全大写 + 短横线）

## 六、阶段 4 不做（留阶段 5+）

- 规则动态增减（阶段 5 引入多 LLM 投票）
- 自动 lint 在 commit 时触发（阶段 5+ 接 lefthook）
- 规则冲突检测（阶段 9 监控时实现）

## 七、Commit 节奏

```
phase start: 阶段4 - 决策契约 + RULE_REGISTRY
feat(规则-府): 14 条规则 + RULE_REGISTRY + 派生值 RULE_COUNT
feat(规则-府): 评估接单 + 决策契约字段校验
chore(质量门禁): 验证-决策契约.ps1 lint 脚本
chore(workspace): 决策契约校验接入事件流写入
phase end: 阶段4 - falsifiable 验证 14/14
```

## 八、风险与回滚

- **风险 1**：14 条规则定义与文档 § 17 不一致 → 回滚到 Day 1 起点，按 AGENTS.md § 17 字面重写
- **风险 2**：lint 脚本正则与 YAML frontmatter 多样格式不兼容 → 简化匹配（只匹配前 --- 至第二个 --- 之间的内容）
- **风险 3**：决策契约字段校验过严导致历史文档无法 lint → 加 `--legacy` 标志跳过往期（仅阶段 4 决策契约启用）

## 九、入稿点验证

写代码前必须先验证本文档已包含：
- [x] 14 条规则的完整列表（13 接单门 + 1 V-COUNT）
- [x] 每条规则的严格度 + 治理域 + 内容
- [x] 决策契约 7 必填字段
- [x] falsifiable 清单
- [x] Day 1-5 任务分解
- [x] commit 节奏
- [x] 风险与回滚

---

*阶段 4 实施方案 · 2026-08-26 · 入稿后再落码 · decided_by: 界主 · 执行: MiniMax-M3*
*implements: 法·可演化（RULE_REGISTRY 单一真相源 + 派生值 + 决策契约强制）*
