# 阶段 5 实施方案：4 分类 + 4 阶段状态机

> **决策锚**：260826-2210 4-分类抽象 + 260826-2240 传承殿启动
> **关联文档**：02-概念/流水线/02-流水线.md + 04-设计/状态机/01-流水线.md + 00-宪法/开发遵守格式模板.md
> **状态**：入稿（2026-08-26）

## 一、目标与 falsifiable

### 1.1 阶段目标

建立业务骨架（无 LLM 也能跑通）：
- 4 分类机械判定（道祖/圣人/准圣/大罗金仙）
- 4 阶段状态机（化要求 → 设计 → 执行 → 验收）
- 不可跳层 + 不可反序（道祖不能直接验收 / 大罗不能先于圣人设计）
- 循环打回 ≤3 轮自动升级道祖终裁

### 1.2 falsifiable 验证清单

- 4 分类机械判定覆盖率 = 100%（无 LLM 调用即可判定）
- 跳层次数 = 0（道祖→准圣、圣人→大罗等跳层全部拒绝）
- 反序次数 = 0（大罗不能先于圣人、验收不能先于执行等）
- 1 个 e2e 测试通过（无 LLM，纯机械 4 分类任务全链路）
- 决策契约 100% 拦截（decided_by 缺失 = 100% 拒绝）

## 二、设计依据

- **概念层**：02-概念/流水线/02-流水线.md（不可跳层 + 不可反序 + 循环打回 ≤3 轮）
- **概念层**：02-概念/角色分类/04-角色分类.md（4 分类抽象）
- **数据模型**：04-设计/状态机/01-流水线.md（4 阶段跳转 + 状态机）
- **决策契约**：04-设计/接口契约/01-插件-manifest.md（decided_by 必填）
- **命名哲学**：00-宪法/开发遵守格式模板.md（中文 + 拼音 + 无 src/ 平铺）

## 三、实施任务（5 天）

### Day 1：扩展 liushuixian_qudong_fu 状态机

**当前状态**：阶段 1 已实现 `阶段 enum { 道祖, 圣人, 大罗, 准圣 }` + `跳转 enum` + `下一阶段(当前, 请求) -> Option<阶段>`（4 项测试）。**待扩展**：

- 4 分类 ↔ 4 阶段映射表（道祖 = 化要求 / 圣人 = 设计 / 大罗 = 执行 / 准圣 = 验收）
- `分类_默认阶段(分类) -> 阶段` 函数（接单时按分类起跳）
- `阶段_分类(阶段) -> 分类` 反向函数
- 不可跳层判定：`is_跳层(从, 到) -> bool`（道祖→准圣、圣人→大罗 都是跳层）
- 不可反序判定：`is_反序(从, 到) -> bool`（大罗→圣人、验收→设计 都是反序）
- 循环打回计数：`循环打回计数(任务ID) -> u32` + 升级道祖终裁（≥3 轮时）

**单元测试**：跳层/反序/循环打回/升级道祖终裁 各 ≥3 个 case。

### Day 2：扩展 renwu_zhixing_fu 角色卡

**当前状态**：阶段 1 已实现 `角色分类 enum`（道祖级/圣人级/准圣级/大罗金仙级）+ `角色卡 struct`（含 decided_by）。**待扩展**：

- 4 张角色卡（道祖卡/圣人卡/准圣卡/大罗卡）的具体内容：
  - 道祖卡：LLM 池 = 化要求专用 / 工具偏好 = 追问引擎 / 权限 = 派遣+终裁 / decided_by = 界主
  - 圣人卡：LLM 池 = 设计专用 / 工具偏好 = 评审四元组 / 权限 = 设计+评审 / decided_by = 界主
  - 准圣卡：LLM 池 = 验收专用 / 工具偏好 = 六维验收 / 权限 = 验收+打回 / decided_by = 界主
  - 大罗卡：LLM 池 = 实现专用 / 工具偏好 = 路径前缀校验 / 权限 = 实现 / decided_by = 界主
- `分类_角色卡(分类) -> 角色卡` 工厂函数
- `分类_机械判定(任务, 分类) -> Result<(), 错误>` 4 分类硬编码（无 LLM 即可调用）

**单元测试**：4 张卡必填字段全覆盖 + 机械判定通过/拒绝 case 各 ≥3 个。

### Day 3：决策契约代码化（接建议 #3）

- `guize_fu::评估接单(候选)` 在 `jiyi_chengzai_fu::写入校验` 之前调用
- `guize_fu::违反(规则, 候选)` 替代现有 jiyi_chengzai_fu 的 decided_by 校验（去重）
- `kuawai_shijian_zongxian_fu::写入` 增加 decided_by 校验（`guize_fu::评估接单`）
- 集成测试：100 个缺 decided_by 写入 → 100% 拒绝（已部分测，扩到事件流）

### Day 4：e2e 任务 + 1 个简单任务全链路

- `mingling_caozuo_fu` 扩展 `run --task=<name>` 命令（CLI 实命令化）
- e2e 任务：界主发布"修复 typo"任务
- 全链路：道祖化要求 → 圣人设计 → 大罗执行 → 准圣验收 → 道祖终裁
- 拒绝路径：跳层（道祖→准圣直接验收）= 拒绝 + 报错
- 循环打回：准圣打回大罗 3 次 → 第 4 次自动升级道祖终裁

### Day 5：commit + 复查

- 复查清单（按模板 § 八）：
  - [ ] 命名全中文（目录）/ 拼音（Rust crate）
  - [ ] 无 `src/` `tests/` `scripts/` 平铺
  - [ ] 无 `.vscode/` `.idea/` 等 IDE 配置
  - [ ] 6 层结构完整（维度/域/府/殿/阁/园）
  - [ ] 中文标识符（Rust 允许）
  - [ ] `cargo lint` + `cargo test` 双通过
  - [ ] 一键全验 11/11 全过
  - [ ] Cargo.toml 用 `path` 桥接到中文入口
  - [ ] 决策文档带 decided_by + falsifiable + implements
  - [ ] 每个 crate 至少 1 个 trait + 1 个测试
- commit `feat(流水线驱动+任务执行): 阶段 5 - 4 分类机械判定 + 4 阶段状态机`
## 四、命名与代码风格契约（按模板）

- **目录**：维度/域/府/殿/阁/园 6 层全中文
- **Rust lib 名**：拼音 snake_case ASCII（liushuixian_qudong_fu / renwu_zhixing_fu 不变）
- **文件**：`入口.rs`（无 src/ 平铺，遵循阶段 1 约定）
- **注释**：中文，含决策锚 + 关联文档 + falsifiable
- **错误**：使用 `错误` enum + `#[derive(thiserror::Error)]`（阶段 2 已落地）
- **测试**：每新增接口至少 3 个测试（happy path + 边界 + 失败）

## 五、阶段 5 不做（留阶段 6+）

- 真实 LLM 调用（阶段 7 接入）
- 多 LLM 投票（阶段 8 追问引擎时实现）
- SQLite 持久化（阶段 3 留接口，阶段 9+ 接入）
- 监控 + 告警（阶段 9 实施）

## 六、Commit 节奏

```
phase start: 阶段5 - 4 分类 + 4 阶段状态机
feat(流水线驱动): 状态机扩展 - 4 分类映射 + 跳层/反序/循环打回
feat(任务执行): 4 张角色卡 + 分类机械判定
chore(质量门禁): 决策契约代码化（guize_fu 接入写入路径）
feat(命令操作): run --task CLI + 1 个 e2e 任务
phase end: 阶段5 - falsifiable 验证
```

## 七、风险与回滚

- **风险 1**：4 分类机械判定覆盖不全 → 补单元测试 case（参考 day 1 任务清单）
- **风险 2**：跳层/反序判定逻辑错 → 加端到端 e2e 验证（day 4 任务）
- **风险 3**：循环打回升级道祖终裁的判定与用户期望不一致 → 与界主确认具体阈值（默认 3 轮）

## 八、入稿点验证

写代码前必须先验证本文档已包含：
- [x] 4 分类 + 4 阶段映射
- [x] 不可跳层/不可反序/循环打回 判定函数签名
- [x] 决策契约代码化（guize_fu 接入）
- [x] 1 个 e2e 任务（界主发布"修复 typo"）
- [x] falsifiable 4 项硬指标
- [x] Day 1-5 任务分解
- [x] 命名风格契约（按模板）
- [x] Commit 节奏

---

*阶段 5 实施方案 · 2026-08-26 · 入稿后再落码 · decided_by: 界主 · 执行: MiniMax-M3*
*implements: 法·4 分类（道祖/圣人/准圣/大罗金仙）+ 4 阶段状态机（不可跳层+不可反序+循环打回≤3 轮）*
*falsifiable: 上线 1 个月，4 分类机械判定覆盖率 = 100% + 跳层次数 = 0 + 反序次数 = 0 + 1 个 e2e 测试通过*

