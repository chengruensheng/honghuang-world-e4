# v4 阶段 17 实施方案：真实 LLM 接入（mock HTTP server）

> **决策锚**：v4 路线阶段 17
> **关联文档**：传承殿/10-地基/09-v4路线.md + 02-概念/可插拔/01-可插拔.md + 04-设计/01-架构总览.md
> **状态**：入稿（2026-08-26）

## 一、目标与 falsifiable

### 1.1 阶段目标

让 moxing_fu 通过真实 HTTP 调用 LLM（用本地 mock HTTP server 替代）：
- moxing_fu 扩展：HTTP连接 支持本地 mock URL + 端点路径
- shishi_fu crate：本地 mock HTTP server（端口可配 + 返回 4 分类 LLM 响应）
- 集成 mingling_caozuo_fu CLI run 命令：跑通真实 HTTP 走 LLM
- 决策契约验证：100 缺 decided_by → 100% 拒绝

### 1.2 falsifiable 验证清单

- shishi_fu crate + 本地 HTTP server 启动 + 端口可配
- moxing_fu HTTP连接 指向 shishi_fu URL，4 分类 LLM 各调用 1 次成功
- CLI run --task=xxx 跑通 mock LLM e2e
- 100 缺 decided_by → 100% 拒绝（决策契约保证）
- ≥ 5 项单元测试 + 一键全验 11/11

## 二、设计依据

- 概念层：02-概念/可插拔/01-可插拔.md（HTTP 连接可换）
- 工程层：阶段 7 moxing_fu 已落地 HTTP POST + OpenAI 兼容
- 工程层：阶段 16 jianyan_gongju（架构校验 Rust 化）
- 命名哲学：哲学层 → 概念层 → 工程层（实时 = shishi_fu）

## 三、实施任务（2 天）

### Day 1：shishi_fu mock HTTP server

- 新建 `道果树/运营-域/实时-府/` crate（shishi_fu）
- `实时-服务` struct：
  - `启动(端口) -> () -> Result<()`
  - `停止() -> ()`
  - 路由 `/v1/chat/completions` → 返回 OpenAI 兼容 JSON
  - 4 分类 mock 响应（道祖/圣人/准圣/大罗）
- 启动后用 curl 测试能拿到 4 分类响应
- 单元测试：启动 + 4 分类响应（用 ureq 调用本地）
- 依赖：ureq（已有）+ serde_json

### Day 2：moxing_fu + mingling_caozuo_fu 集成

- moxing_fu HTTP连接 端点配置：支持环境变量 `MOCK_LLM_URL`
- mingling_caozuo_fu::跑流水线_mock_llm 改为：使用 moxing_fu + 真实 HTTP
- e2e 测试：CLI run --task=test 走 mock HTTP
- 决策契约 100 缺 decided_by → 100% 拒绝（继承阶段 12 验证）

### Day 3：commit + 一键全验

- cargo test 全过
- cargo clippy 0 警告
- 一键全验 11/11 全绿
- commit `feat(shishi_fu): v4 阶段 17 - 真实 LLM mock HTTP`

## 四、命名与代码风格契约

- 目录：道果树/运营-域/实时-府/（运营/域/府 3 段）
- Rust lib 名：shishi_fu（实时 = shishi）
- 文件：入口.rs（无 src/ 平铺）
- 注释：中文，含决策锚
- 依赖：ureq + serde_json + thiserror

## 五、不做（留 v4 阶段 19）

- 真实云端 LLM 接入（需 API key）
- 异步 e2e
- 性能基准（v4 阶段 15）

## 六、Commit 节奏

阶段 17 Day 1：shishi_fu mock server
阶段 17 Day 2：moxing_fu 集成 + e2e
阶段 17 Day 3：commit phase end

## 七、风险与回滚

- 风险 1：mock server 端口冲突 → 用 OS 分配端口（0 让系统选）
- 风险 2：HTTP 异步复杂 → 用 std::net::TcpListener + 手动 HTTP 解析（不依赖 tokio）
- 风险 3：moxing_fu 真实 URL 配置 → 默认 localhost:port，环境变量覆盖

## 八、入稿点验证

- shishi_fu crate 4 分类 mock 响应
- moxing_fu HTTP连接 环境变量支持
- mingling_caozuo_fu run 走真实 HTTP
- 决策契约 100% 拒绝（继承阶段 12）

---

*阶段 17 实施方案 · 2026-08-26 · 入稿 · decided_by: 界主 · 执行: MiniMax-M3*
*falsifiable: 1 周内，mock server 启动 + CLI run 真实 HTTP 走通 + 决策契约 100% 拒绝*
