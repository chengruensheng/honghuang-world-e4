# v4 阶段 18 实施方案：多 agent 协同

> **决策锚**：v4 路线阶段 18
> **关联文档**：传承殿/10-地基/09-v4路线.md + 02-概念/角色分类/04-角色分类.md + 04-设计/状态机/01-流水线.md
> **状态**：入稿（2026-08-26）

## 一、目标与 falsifiable

### 1.1 阶段目标

让道祖级可同时调度多个大罗级任务：
- 道祖级调度器：管理 ≥ 2 大罗级任务
- 多任务并行：每个任务独立 4 分类流水线
- 任务跟踪：每任务状态（提案/执行/完成）+ 时间戳
- 错误隔离：单任务失败不影响其他任务

### 1.2 falsifiable 验证清单

- diaoqian_fu crate + 调度器 + ≥ 5 单元测试
- 2 大罗级任务并行跑完 4 分类流水线
- 任务完成率 = 100%
- 单任务错误不影响其他任务
- ≥ 1 单元测试验证并行
- 一键全验 11/11 全绿

## 二、设计依据

- 概念层：02-概念/角色分类/04-角色分类.md（4 分类 + 角色卡）
- 工程层：阶段 5 liushuixian_qudong_fu（4 分类状态机）
- 工程层：阶段 10 shengji_fu（3 类升级策略）
- 命名哲学：调遣 = diaoqian_fu（哲学层 → 概念层 → 工程层）

## 三、实施任务（3 天）

### Day 1：diaoqian_fu crate 骨架

- 新建 `道果树/运营 - 域/调遣 - 府/` crate（diaoqian_fu）
- `调度器` struct：管理多个任务 + 状态机
- `任务项` struct：任务ID + 状态 + 开始时间 + 完成时间
- `调度(任务)` 方法：把任务加入调度器
- `运行_全部()` 方法：顺序跑所有任务
- ≥ 3 单元测试（添加/运行/状态）

### Day 2：集成 liushuixian_qudong_fu

- `运行_任务(任务项)`：调 liushuixian_qudong_fu 跑 4 分类流水线
- 决策契约：每个任务必须带 decided_by
- 错误隔离：try/catch 单任务失败不影响其他
- ≥ 2 单元测试（单任务跑通 + 错误隔离）

### Day 3：commit + 一键全验

- cargo test 全过
- cargo clippy 0 警告
- 一键全验 11/11
- commit `feat(diaoqian_fu): v4 阶段 18 - 多 agent 协同`

## 四、命名与代码风格契约

- 目录：道果树/运营 - 域/调遣 - 府/
- Rust lib 名：diaoqian_fu
- 文件：入口.rs
- 注释：中文，含决策锚
- 依赖：serde + thiserror

## 五、不做

- 真实并行（tokio / std::thread 多线程）—— 简化为顺序模拟
- 任务调度算法（轮询 / 优先级）—— 简化为 FIFO

## 六、Commit 节奏

阶段 18 Day 1：diaoqian_fu 骨架
阶段 18 Day 2：集成 + e2e
阶段 18 Day 3：commit phase end

## 七、风险与回滚

- 风险 1：任务调度逻辑复杂 → 简化为顺序 FIFO，2 任务即可证明
- 风险 2：状态机冲突 → 每任务独立 4 分类流水线，互不干扰
- 风险 3：测试隔离 → 用 Cargo test 顺序保证

## 八、入稿点验证

- diaoqian_fu crate + 调度器 struct
- ≥ 2 大罗级任务并行跑完
- 任务完成率 100%
- 错误隔离
- ≥ 5 单元测试

---

*阶段 18 实施方案 · 2026-08-26 · 入稿 · decided_by: 界主 · 执行: MiniMax-M3*
*falsifiable: 道祖级调度 ≥ 2 大罗级任务 100% 完成 + 11/11 全绿*
