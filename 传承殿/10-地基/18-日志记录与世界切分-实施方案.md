# 18-日志记录与世界切分-实施方案

> **决策锚**：260827-统一风格（Round 4）
> **关联文档**：16-统一六层风格-实施方案.md + 14-命名唯一性门禁-实施方案.md + 17-单元测试与配置管理切分-实施方案.md
> **状态**：✅ Round 4 完成（2026-08-27 · 全部 7 府级 crate 切分完成）

## 一、本轮切分目标

| 府 | 行 | 殿 | 阁 | 园 | 本轮 |
|:--|:--|:--|:--|:--|:--|
| 日志记录 | 56 | 3 | 4 | 4 | ✅ Round 4 |
| 世界 | 19 | 2 | 3 | 3 | ✅ Round 4（需扩内容） |

## 二、日志记录-府 切分（56 行）

```
日志-接口-殿/
├── 接口-契约-阁/日志契约-园/         trait 日志（Send+Sync）
└── 方法-要件-阁/日志要件-园/         trait 方法签名说明
级别-定义-殿/
└── 级别-枚举-阁/级别枚举-园/         enum 级别 + Debug/PartialEq/Eq
输出-实现-殿/
├── 结构-定义-阁/输出结构-园/         struct 标准输出日志
└── 输出-实现-阁/输出实现-园/         impl 日志 + 新建 + 前缀匹配
```

## 三、世界-府 切分（19 行 → 扩内容后切 2 殿 3 阁 3 园）

**原内容**：`fn 就绪() -> bool` + 二进制 main 打印固定字符串。

**扩内容**：加 `fn 成员清单() -> Vec<&str>` 返回 crate 名 + `fn 版本() -> &str`。

```
就绪-检测-殿/
└── 就绪-方法-阁/就绪-园/             fn 就绪() -> bool + 测试
工作空间-状态-殿/
├── 成员-清单-阁/成员-园/             fn 成员清单() -> Vec<&str>
└── 工作空间版本-阁/工作空间版本-园/  fn 版本() -> &str
```

二进制入口 main 改为调用 `世界::成员清单().len()` 输出动态 crate 数。

## 四、命名唯一性校验

- 跨府无重名：日志契约-园/日志要件-园/级别枚举-园/输出结构-园/输出实现-园 与现有命名无冲突
- 跨府唯一：工作空间版本-阁/工作空间版本-园（避免与状态共享的 版本-递增-园/版本-查询-园 混淆）

## 五、风险与回滚

- 风险 1：世界 crate 二进制 main 输出依赖静态字符串 → 改为调用 fn 成员清单() 动态输出
- 回滚：每个 crate 一个独立 commit

## 六、falsifiable

- [x] 日志记录-府 3 殿 4 阁（命名门禁拦截后改 日志契约-阁/输出-结构-阁/输出-方法-阁/日志契约定义-园/输出结构定义-园/输出方法-园）
- [x] 世界-府 2 殿 3 阁（扩内容：fn 成员清单() 返回 21 crate + 工作空间版本；二进制入口改用动态 API）
- [x] cargo check + 211 项测试全过 + clippy 零警告
- [x] 一键全验 12/12 全绿（命名门禁拦截 6 处违规后全部修复）

---

*18-日志记录与世界切分-实施方案 · 2026-08-27 · 入稿 · decided_by: 界主*
*implements: 术（统一六层风格收尾）*
*falsifiable: 2 crate 全部 ≥2 殿 ≥3 阁 + 12/12 门禁*
