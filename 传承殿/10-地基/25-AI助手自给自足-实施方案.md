# 25-AI 助手自给自足-实施方案（Round 11）

> **决策锚**：260827-AI助手自给自足
> **关联文档**：24 号决策 + 14-命名唯一性门禁 + 16-统一六层风格 + AGENTS.md
> **状态**：✅ 入稿（2026-08-27）

## 一、目标

让 AI 助手（DSH）能**不需人类额外提醒**就完美遵守本项目所有开发规则规范：36 格位真正应用（哲学层→概念层→工程层 三段推导入库 + AI 自动加载）、命名哲学白名单、祖孙三层判据、≥2 殿拒绝单链、≥10 项一键全验通过——生成符合项目规则的代码端到端通过门禁。

## 二、当前差距

| 项 | 现状 | 目标 |
|:--|:--|:--|
| 36 格位 | 数据结构存在（jiyi_chengzai_fu） | AI 开始任务前自动加载相关格位生成符合契约的代码 |
| 命名哲学 | jianyan_gongju 第 12 项校验存在 | AI 直接读取白名单（SQLite/P0-P3）生成符合命名 |
| CLAUDE.md/AGENTS.md | 仅项目约束 | 加 AI 助手自给自足章节 |
| 端到端自开发 | Round 10 demo（minimax 5.3s + 1 文件） | AI 助手无人类提示生成完整 6 层结构 crate |
| 36 格位写入 | 仅人工手动 | 每个决策自动写入 |

## 三、实施步骤

### Step 1：CLAUDE.md / AGENTS.md 自给自足化
- 加 § 18 → § 25 条 + AI 助手必读章节
- 5 个必读文档（README + 16 号 + 14 号 + 04-设计/命名哲学 + jianyan_gongju 输出）
- 命名白名单（SQLite/P0-P3 + 命名判据）
- 自动检查命令（一键全验 + clippy + 决策契约 lint）

### Step 2：36 格位写入 + 读取闭环
- jiyi_chengzai_fu 加 写入_格位(范畴, 阶段, 内容, 来源) API
- jiyi_chengzai_fu 加 读取_相关格位(任务描述) → 返回相关格位内容
- 决策契约 9 文件全部迁移到 36 格位（每文件对应 1+ 范畴+阶段组合）
- AI 助手开始任务前自动调用 读取_相关格位() 获取上下文

### Step 3：CLAUDE.md / AGENTS.md 写明 36 格位 + jianyan_gongju 调用方式
- mingling_caozuo_fu 加 36 格位查询 命令（CLI 子命令）

### Step 4：端到端真实生成（Round 11 增强）
- curl + 文件 body + retry + max_tokens 8000-16000 + 输出解析器（=== FILE: 路径 === 格式）
- 强制 6 层结构 ≥2 殿 ≥2 阁 ≥1 园 + 祖孙三层判据 + cargo check + cargo test + 一键全验 13/13
- 真实可用新 crate（非 demo）

## 四、falsifiable

- [ ] AI 助手无人类提示能生成新 crate（满足 6 层 + 命名 + 门禁）
- [ ] AI 助手开始任务前自动加载 36 格位（无需人类提醒）
- [ ] 36 格位 ≥10 条决策自动写入（按范畴+阶段覆盖）
- [ ] ≥10 个真实任务跑通（按可用阶段判定标准）
- [ ] 一键全验 13/13 + cargo clippy 零警告

## 五、风险与回滚

- 风险 1：AI 助手可能不读 36 格位（CLAUDE.md 强约束）
- 风险 2：端到端真实生成 LLM 输出仍受 token 限制
- 回滚：CLAUDE.md 修改 git revert；36 格位写入 API 可 disable

---

*25-AI助手自给自足-实施方案 · 2026-08-27 · 入稿 · decided_by: 界主*
*implements: 鉴·自给自足（AI 助手按项目规则自动生成可用代码）*
*falsifiable: AI 助手无人类提示生成完整 6 层结构新 crate + 一键全验 13/13*
