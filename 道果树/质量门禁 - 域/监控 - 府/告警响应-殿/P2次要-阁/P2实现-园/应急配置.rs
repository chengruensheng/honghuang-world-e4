//! P2 次要应急配置 - P2 级告警的应急流程配置
//!
//! 对应应急级::准圣级，响应模式::半自动

use crate::告警响应_殿::响应模式;

/// P2 次要级应急配置：(动作, 负责人, 响应模式)
pub fn 应急配置() -> (&'static str, &'static str, 响应模式) {
    ("验收后回滚或补丁", "准圣级负责人", 响应模式::半自动)
}
