[package]
name = "renwu_zhixing_fu"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "任务执行-府 - LLM 池 + 4 分类角色卡 + 自举任务单（接 02-概念/规则注册表/06-规则注册表.md）"

[lib]
name = "renwu_zhixing_fu"
path = "入口.rs"

[dependencies]
serde = { workspace = true }
# 自举任务单校验复用工具府命令/路径白名单（治理铁律：验收命令与目标文件受白名单约束）
gongju_fu = { path = "../工具调用-府" }


