//! 契约 - 探针特征定义
//!
//! 决策锚：260826-2230 工程-DSH § 一键全验
//! 关联文档：07-运营/监控/02-监控.md

/// 探针特征：所有可埋点的组件必须实现
pub trait 探针: Send + Sync {
    /// 探针名（与埋点通道一一对应）
    fn 名称(&self) -> &str;
    /// 自增 1 并返回新值
    fn 计数(&self) -> u64;
    /// 当前计数
    fn 当前(&self) -> u64;
}
